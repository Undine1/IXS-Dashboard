# Repository workflow preference

- After successfully implementing and verifying requested repository changes, commit them, push the current branch, and deploy the pushed revision to production unless the operator explicitly says not to.
