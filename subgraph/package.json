{
  "name": "ixs-usdc-subgraph",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy:auth": "graph auth --product hosted-service $GRAPH_API_KEY",
    "deploy": "graph deploy --product hosted-service $GITHUB_USER/$SUBGRAPH_NAME"
  }
}
